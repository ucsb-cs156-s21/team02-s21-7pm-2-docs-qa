import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import AppFooter from "main/components/Footer/AppFooter";

<Meta title="components/Footer/AppFooter" component={AppFooter} />

# AppFooter

The `AppFooter` appears at the bottom of each page of the app.

export const Template = (args) => <AppFooter {...args} />

<Canvas>
  <Story name="Example" args={{ label: 'Example' }}>
    {Template.bind({})}
   </Story>
</Canvas>
